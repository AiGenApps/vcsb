import 'package:flutter/material.dart';

class SyncManagementPage extends StatelessWidget {
  const SyncManagementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('同步管理页面'));
  }
}
